-- =============================================================
-- 账户级 UI 偏好:每行列数(grid_cols)
--   NULL = 该账户从未保存过,前端按设备取默认值(PC 12 / 手机 3)
--   非 NULL = 用户上次保存的列数,跨设备/跨浏览器跟随账户生效
-- =============================================================

ALTER TABLE users ADD COLUMN grid_cols INTEGER;
